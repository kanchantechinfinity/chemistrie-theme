{% render 'page-cta',
  eyebrow: section.settings.eyebrow,
  heading: section.settings.heading,
  sub: section.settings.sub,
  sub2: section.settings.sub2,
  button_label: section.settings.button_label,
  button_url: section.settings.button_url,
  button2_label: section.settings.button2_label,
  button2_url: section.settings.button2_url
%}

{% schema %}
{
  "name": "Page CTA",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "— Ready when you are —" },
    { "type": "html", "id": "heading", "label": "Heading (supports <em>/<br>)", "default": "Begin your <em>ritual.</em>" },
    { "type": "textarea", "id": "sub", "label": "Subtext", "default": "Five formulas, hand-numbered in Houston. Find the one your skin has been waiting for." },
    { "type": "textarea", "id": "sub2", "label": "Subtext line 2 (optional)" },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Shop the Collection" },
    { "type": "url", "id": "button_url", "label": "Button URL" },
    { "type": "text", "id": "button2_label", "label": "Secondary button label (optional)" },
    { "type": "url", "id": "button2_url", "label": "Secondary button URL" }
  ],
  "presets": [{ "name": "Page CTA" }]
}
{% endschema %}
